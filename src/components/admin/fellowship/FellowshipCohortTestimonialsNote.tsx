'use client'

import '../hero/hero-slides-list-banner.scss'

export function FellowshipCohortTestimonialsNote() {
  return (
    <div className="hero-slides-list-banner mb-4 rounded-lg border border-[#e8dcc5] bg-[#faf6f0] px-4 py-3 text-sm text-[#002740]">
      <p className="mb-0">
        Testimonials appear on the cohort page only when <strong>Show testimonials section</strong>{' '}
        is enabled above. Adding a testimonial below will turn that on automatically when you save.
      </p>
    </div>
  )
}
